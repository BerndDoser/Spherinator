version: v1.0
workflows:
  hipster:
    type: cwl
    config:
      file: hipster.cwl
    bindings:
      - step: /
        target:
          deployment: slurm

deployments:
  slurm:
    type: slurm
    config:
      hostname: cascade
      username: doserbd
      file: sbatch.sh
