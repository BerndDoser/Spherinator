version: v1.0
workflows:
  hipster:
    type: cwl
    config:
      file: hipster.cwl
    bindings:
      - step: create_images
        target:
          model: slurm

models:
  slurm:
    type: slurm
    config:
      hostname: cascade
      username: doserbd
