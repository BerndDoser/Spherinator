version: v1.0
workflows:
  hipster:
    type: cwl
    config:
      file: hipster.cwl
    bindings:
      - step: /
        target:
          model: slurm

models:
  slurm:
    type: slurm
    config:
      hostname: cascade
      username: doserbd
      file: sbatch.sh
