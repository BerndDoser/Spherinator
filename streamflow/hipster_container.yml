version: v1.0
workflows:
  hipster:
    type: cwl
    config:
      file: hipster.cwl
    bindings:
      - step: generate_hips
        target:
          model: docker-ubuntu

models:
  docker-ubuntu:
    type: docker
    config:
      image: spherinator:latest
