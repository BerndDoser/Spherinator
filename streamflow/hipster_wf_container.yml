version: v1.0
workflows:
  hipster:
    type: cwl
    config:
      file: hipster.cwl
    bindings:
      - step: generate_hips
        target:
          model: container

models:
  container:
    type: docker
    config:
      image: ghcr.io/bernddoser/spherinator:container
