version: v1.0
workflows:
  hipster:
    type: cwl
    config:
      file: hipster.cwl
    bindings:
      - step: /
        target:
          model: container

models:
  container:
    type: docker
    config:
      image: ghcr.io/bernddoser/spherinator:container
      volume:
       - /hits/basement:/hits/basement
      user: "15005"
