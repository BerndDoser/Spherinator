version: v1.0
workflows:
  hipster:
    type: cwl
    config:
      file: hipster.cwl
    bindings:
      - step: create_images
        target:
          model: container

models:
  container:
    type: docker
    config:
      image: ghcr.io/bernddoser/spherinator:container
